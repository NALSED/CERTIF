## Metrics Server

- Composant qui collecte les `métriques CPU/RAM` des `nodes` et `pod`.

- Il sert aussi de base pour l'autoscaling (HPA — [Horizontal Pod Autoscaler](https://kubernetes.io/docs/concepts/workloads/autoscaling/horizontal-pod-autoscale/)), qui a besoin de ces métriques pour décider quand scaler un déploiement

---

`[NOTE]`

- Le metric server n'est pas toujours installé par `kubeadm`

[REPO_GITHUB](https://github.com/kubernetes-sigs/metrics-server)

=== Pour l'installer : ===
````
kubectl apply -f https://github.com/kubernetes-sigs/metrics-server/releases/latest/download/components.yaml
````

````
k get -n kube-system pod

# Sortie
metrics-server-687f9dc499-f8dcs            0/1     Running   0             6m17s
````

- La raison est la suivante metric-server n'est pas configuré pour comunique en `TLS`

- Pour régler le probléme
````
kubectl -n kube-system edit deployment metrics-server
#Editer
spec:
  container
  - args
    - --kubelet-insecure-tls <========= Cette ligne
````

- Maintenant
````
kubectl get pods -n kube-system
# Sortie
metrics-server-589cd6fccb-89tvv            1/1     Running   0              6m8s
````
````
k top nodes
NAME          CPU(cores)   CPU(%)   MEMORY(bytes)   MEMORY(%)
k8s-master    72m          1%       2069Mi          29%
k8s-worker1   25m          0%       1146Mi          22%
k8s-worker2   18m          0%       1002Mi          19%
````
