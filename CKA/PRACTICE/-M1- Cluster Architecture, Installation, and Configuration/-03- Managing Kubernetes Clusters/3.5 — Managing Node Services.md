## Gestion Node service

---

- `CRI` (ici containerd) et `kubelet` sont géré par `systemd`

 ---

 - Pour voir l'état utiliser les commande traditionnelles de linux :
````
sudo systemctl status/start/stop kubelet

ps aux

crictl ps
````
